This endpoint synchronizes overdue DFC flags and creates one-shot in-app notifications.

Call:
- GET /api/cron/dfc-overdue with Authorization: Bearer <CRON_SECRET>
- or GET /api/cron/dfc-overdue as ADMIN user session
